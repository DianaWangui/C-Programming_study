#include <stdio.h>
#include <conio.h>
int main()
{
    int a;

    printf("enter the values of a and b: ");
    scanf("%d", &a);

    if (a > 0)
        printf("Its number %d \n", a);

    printf("Its not a number");
}