#include <stdio.h>
int main()
{
	int width = 4, length = 6, area;

	// printf("enter length and width: ");
	// scanf("%d %d", &width, &length);

	area = 0.5 * length * width;
	printf("The are is: %d\n", area);
	return 0;
}
